import os
import time

# Execute the shutdown command (requires root or sudo permissions)
# time.sleep(10)
print("Shutdown")
# os.system("sudo shutdown -h now")

# We want to simulate a shutdown like above